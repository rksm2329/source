#include <bits/stdc++.h>

using namespace std;

const int MAXN = 2e5 + 10;

int t, n, a[MAXN];

void Solve() {
  cin >> n;
  for (int i = 1; i <= n; i++) {
    cin >> a[i];
  }
  for (int i = 2, x; i <= n; i++) {
    cin >> x;
  }
}

int main() {
  ios::sync_with_stdio(0), cin.tie(0);
  for (cin >> t; t--; Solve()) {
  }
  return 0;
}