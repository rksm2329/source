#include <bits/stdc++.h>

using namespace std;
using ll = long long;

ll r;

int main() {
  ios::sync_with_stdio(0), cin.tie(0);
  cin >> r;
  cout << 2 * r * (r - 1) * floor(sqrt(r * r - ()))
  return 0;
}