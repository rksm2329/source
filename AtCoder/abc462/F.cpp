#include <bits/stdc++.h>

using namespace std;
using ll = long long;

const int MAXN = 3e5 + 10;

string s;
int k, dp[MAXN][10][3];

void Solve() {
  cin >> s >> k;
  int n = s.size();
}

int main() {
  cin.tie(0)->sync_with_stdio(0);
  int T;
  for (cin >> T; T--; Solve());
  return 0;
}