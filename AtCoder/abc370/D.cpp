#include <bits/stdc++.h>

using namespace std;

int h, w, q;

int main() {
  ios::sync_with_stdio(0), cin.tie(0);
  cin >> h >> w >> q;
  for (int r, c; q; q--) {
    cin >> r >> c;
    
  }
  return 0;
}