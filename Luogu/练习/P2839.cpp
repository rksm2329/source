#include <bits/stdc++.h>

using namespace std;
using ll = long long;

const int MAXN = 2e4 + 10;

int n, a[MAXN];

int main() {
  cin.tie(0)->sync_with_stdio(0);
  cin >> n;
  for (int i = 1; i <= n; cin >> a[i++]);
  return 0;
}